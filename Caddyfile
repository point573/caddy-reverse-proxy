{
  auto_https off
}

:{$PORT} {
  header {
    Access-Control-Allow-Origin *
    X-Frame-Options "ALLOWALL"
  }

  reverse_proxy {
    dynamic a {
      name {$FRONTEND_DOMAIN}
      port {$FRONTEND_PORT}
    }
  }
}
